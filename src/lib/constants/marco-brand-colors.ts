/** Brand CTA fill — RGB(63, 84, 102). Matches marco.am / localhost:3001. */
export const MARCO_SLATE_HEX = "#3F5466" as const;

/** Anthracite label on yellow promo pills (e.g. «ԳՆԵԼ ՀԻՄԱ»). */
export const MARCO_ANTHRACITE_HEX = "#383838" as const;

/** Brand yellow — CTAs, accents, active nav pills. */
export const MARCO_YELLOW_HEX = "#FFCA03" as const;

/** Text on solid brand-yellow surfaces (3001: `--text-on-yellow`). */
export const MARCO_TEXT_ON_YELLOW_CLASS = "text-marco-black";

/** Circular icon actions (wishlist, compare) on product cards. */
export const MARCO_SLATE_ICON_CHIP_CLASS =
  "bg-marco-slate text-white shadow-sm transition-[filter] hover:brightness-95 active:brightness-90";

/** Pill CTAs on slate background. */
export const MARCO_SLATE_PILL_BUTTON_CLASS =
  "bg-marco-slate text-white hover:brightness-95 active:brightness-90";
